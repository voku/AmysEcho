{
  "name": "amys-echo-app-proxy",
  "private": true,
  "description": "Proxy package to forward npm --prefix calls when executed from the app directory.",
  "scripts": {
    "type-check": "cd .. && npm run type-check",
    "test": "cd .. && npm test",
    "lint": "cd .. && npm run lint"
  }
}
