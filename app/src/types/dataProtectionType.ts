export interface GestureData {
  gestureClass: string;
  confidence: number;
  timestamp: number;
  sessionId: string;
}

export interface AnonymizedGestureData {
  gestureClass: string;
  confidence: number;
  timestamp: number;
  sessionId: string;
}