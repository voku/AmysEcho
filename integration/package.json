{
  "name": "integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "sh -c 'PY_BIN=$(node ../server/scripts/resolve-python-bin.mjs) && npm ci --include=dev && npm ci --prefix ../webapp && npm ci --prefix ../server && \"$PY_BIN\" -m pip install -r ../server/requirements.txt && npm run build --prefix ../server'",
    "test": "node ./run-tests.mjs"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "24.10.15",
    "adm-zip": "^0.5.16",
    "tsx": "^4.21.0",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "jsonwebtoken": "^9.0.2"
  }
}
