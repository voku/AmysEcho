{
  "name": "integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm ci --include=dev && npm ci --prefix ../server && npm run build --prefix ../server",
    "test": "node ./run-tests.mjs"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "tsx": "^4.20.5"
  },
  "dependencies": {}
}
