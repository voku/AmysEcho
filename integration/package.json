{
  "name": "integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm ci --include=dev && npm run build --prefix ../server",
    "test": "tsx --test \"**/*.{spec.ts,test.js}\""
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
