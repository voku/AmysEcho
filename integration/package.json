{
  "name": "integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm run build:server",
    "build:server": "npm run build --prefix ../server",
    "test": "npm run test:full",
    "test:full": "node ./run-tests.mjs",
    "test:fast": "INTEGRATION_TEST_PROFILE=fast node ./run-tests.mjs"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "24.12.0",
    "adm-zip": "^0.6.0",
    "tsx": "^4.21.0",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "jsonwebtoken": "^9.0.2"
  }
}
