Implemented simplified video controls in `app/src/components/DgsVideoPlayer.tsx` by:
- Making the entire video area a `Pressable` for play/pause toggle.
- Adding a `Play`/`Pause` icon overlay for visual feedback.
- Removing the old control button.
- Mocked `react-native-svg` and `lucide-react-native` in `app/jest.setup.ts` to fix `dgsVideoPlayer.test.tsx`.

Added a TODO comment in `app/src/services/dgsModelClient.ts` regarding certificate pinning, as it requires a non-surgical change (new native module) and is outside the scope of this task.

Verification:
- `npm ci --prefix app`: Passed.
- `npm run type-check --prefix app`: Passed.
- `npm test --prefix app`: `dgsVideoPlayer.test.tsx` now passes. Other unrelated tests continue to fail due to broader Jest configuration issues.
- `npx expo install --check`: Passed.
- `npx expo-doctor`: Reported non-blocking warnings related to `react-native` installation, but the core task's verification is complete.