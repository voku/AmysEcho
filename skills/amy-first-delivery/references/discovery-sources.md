# Discovery Sources

Read these first for Amy's Echo implementation work:

1. `docs/planning/todo.md`
2. `docs/workflows/development-workflow.md`
3. `docs/testing/testing-strategy.md`
4. Root `AGENTS.md` plus the deepest applicable scoped `AGENTS.md` for every touched path (for example `webapp/AGENTS.md` or `server/AGENTS.md`).

Then inspect nearby implementation and tests before coding:

- Webapp: `webapp/src/components/`, `webapp/src/hooks/`, `webapp/src/gesture/`
- Server: `server/src/services/`, `server/src/tools/`, `server/test/`
- Integration: `integration/`
