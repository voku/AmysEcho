/**
 * Entry point for the gesture detector bundle.
 * Builds on the modular implementation in gestureDetector.new.ts.
 * Run `npm run build:webview --prefix app` after modifying this file.
 */
import './gestureDetector.new';
